# Environment configuration for Render deployment
# Copy to .env locally (do NOT commit .env) and set real secrets/values.

ENV=production
LOG_LEVEL=INFO
PORT=5050

# Required: set your real OpenAI API key in Render dashboard env vars
OPENAI_API_KEY=sk-...

# CORS: default deny. Add your production origins comma-separated if needed
# Example after first deploy: https://your-app.onrender.com,https://app.yourdomain.com
ALLOWED_ORIGINS=

# Input size caps
MAX_TURN_CHARS=600
MAX_TTS_CHARS=500

# Rate limits (require flask-limiter installed)
DEFAULT_RATE_LIMIT=100/hour
RATE_LIMIT_TTS=20/minute
RATE_LIMIT_ROLEPLAY_TURN=40/minute
RATE_LIMIT_ROLEPLAY_FEEDBACK=20/minute
RATE_LIMIT_MATCHING=60/minute

# App version
VERSION=1.0.0

# Data collection for pilot study
ENABLE_ANALYTICS=true
ANALYTICS_SAMPLE_RATE=1.0
